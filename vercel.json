{
  "version": 2,
  "builds": [
    {
      "src": "dist/**/*",
      "use": "@vercel/static"
    }
  ],
    "routes": [
      { "src": "/dist/(.*)", "dest": "/dist/$1" },
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/index.html" }
    ]
}
